name: Build and Deploy Code

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main


jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Pulling our code
        uses: actions/checkout@v4
      - name: Say hi to pepsik
        uses: echo "Hello, Pepsik"
